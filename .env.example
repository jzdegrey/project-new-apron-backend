# Copy to `.env` and fill in real values. Never commit the real `.env` file.

# Must be one of: local, stg, prod
ENV=local

APP_NAME=project-new-apron-backend
HOST=0.0.0.0
PORT=8000

# Secrets
SECRET_KEY=change-me

# Database
DB_HOST=db
DB_PORT=3306
DB_NAME=project_new_apron
DB_USER=app
DB_PASSWORD=change-me
